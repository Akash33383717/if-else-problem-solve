let text = "Bangladesh"

for (let i = text.length-1; i >= 0; i--) {
    console.log("letter index "+ i +" is : " + text[i])
}
for (let i = 0; i < text.length; i++) {
    console.log("letter index "+ i +" is : " + text[i])
}